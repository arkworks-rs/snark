language: rust

rust:
  - stable
  - nightly

matrix:
  fast_finish: true

cache: cargo

script:
  - cargo test --all --release -- --skip "plain_dpc" --skip "integration_test"
  - cargo check --examples --all
