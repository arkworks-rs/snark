language: rust

rust:
  - stable
  - nightly

matrix:
  fast_finish: true

cache: cargo

script:
  - cd algebra
  - cargo test
  - cargo check --profile bench
  - cd ../r1cs-core
  - cargo check
  - cd ../r1cs-std
  - cargo test
  - cd ../gm17
  - cargo test
  - cargo check --examples
  - cd ../dpc
  - cargo check --profile test
